name: Sync from FEF Template Repo

on:
  # 1. Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  sync:
    if: ${{ github.event.repository.fork }}
    uses: uc-cdis/.github/.github/workflows/gen3_Sync_FeF.yaml@master
    secrets:
      GH_TOKEN: ${{ secrets.PLANXCYBORG_PAT }}
